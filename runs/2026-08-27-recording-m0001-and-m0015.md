# Run — recording: m0001 re-ratification (m0014 amendment); m0015 acknowledgment

Append-only verification record (spec §9.1). Never edited; a
superseding run is a new file. Filed for the recording of two
operator acts stated in one turn
([execution thread R14](../threads/2026-08-26-m0012-execution.md)):
"I re-ratify m0001 at commit d800ee8" and "I acknowledge m0015 at
commit 60b7a46". This run is the mechanical half — pin computation
and identity checks.

## Claims tested

- The re-ratified document is the text at the operator's named
  commit: `doctrine/matters.md` at the PR #7 merge commit hashes to
  the declared candidate, and the working file matches it.
- The acknowledged m0015 is the text at the operator's named commit,
  and its §11 retroactive-regime pin is reproducible.

## Environment

- OS: Linux 6.18.44-fc-v21, x86_64 (remote agent container)
- bash 5.2.21(1)-release; git 2.43.0; GNU coreutils 9.4
  (`sha256sum`); Python 3.11.15, PyYAML 6.0.1
- Repo: `markreveley/formic-matters`, branch
  `m0015-agent-instructions` (the recordings ride the open m0015 pull
  request, reverse-order practice); `main` at
  `d800ee8a928d220bf7e27cf547d856ac38f4c784`, the PR #7 merge commit.
  The tree verified is this recording's, in which this file lands.

## Commands and results

1. **m0001, whole-file regime:**

   ```
   git show d800ee8a928d220bf7e27cf547d856ac38f4c784:doctrine/matters.md | sha256sum
   sha256sum doctrine/matters.md
   ```

   Expected: both equal the candidate declared in the m0014 execution
   record. **Observed: both
   `e55dd508d7048789a43a2f98e403366da11b1a4f6f14d0bff2766438d98a7381`.**
   Recorded into m0001's frontmatter at 2026-08-27T02:10:06Z, the
   superseded pin (`97854b8…` / `9fb1f925…`) preserved in the
   re-ratification entry.

2. **m0015, §11 retroactive regime** — the body after the
   frontmatter's closing `---`, `## Retroactive` and `## Execution`
   included (no `## Vetting` present at the acknowledged commit),
   sha256 over UTF-8, at `60b7a46`; the file there verified
   byte-identical to the working copy before the recording appends:

   **Observed:
   `06782accfd9f856a272092081cc096b0a6167c58b879689d16544471f939b23e`**
   — recorded into m0015's frontmatter with the acknowledgment;
   regime stated per m0007's name-the-regime requirement.

3. **Index.** `python3 tools/gen-index.py` twice from the repo root:
   byte-identical; ten matters — m0001, m0012, m0014, m0015
   `executed`; m0013 `ratified`; m0006–m0008, m0010, m0011
   `proposed`.

## Verdict

Both acts are recorded with their pins, each independently
recomputable from its named commit. The framework's records carry no
red value: every recorded pin matches its text.

- Date: 2026-08-27
- Actor: claude-code/2026-08-26, the recording agent (the pins are
  the independence mechanism). Thread:
  [2026-08-26-m0012-execution](../threads/2026-08-26-m0012-execution.md)
